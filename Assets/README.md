# Unity Assets

이 디렉터리는 게임 코드, 씬, 프리팹, 아트 리소스를 포함하는 Unity 기본 루트다.

현재 저장소 단계는 `prototype`이며, 실제 구현 파일과 playable vertical slice 자산이 함께 존재한다.

## 현재 운영 기준

- 프로젝트 소유 구현은 `Assets/_Game/**`에 둔다.
- 테스트는 `Assets/Tests/**`에 둔다.
- 원본 vendor 자산은 `Assets/ThirdParty/**`에서 보존하고 직접 수정하지 않는다.
