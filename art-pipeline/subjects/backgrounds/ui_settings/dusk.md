---
slug: ui_settings--dusk
kind: environment_site
subject_id: ui_settings
variant: dusk
emotion: default
refs:
  - hero_dawn_priest:portrait_full
  - town_frontier_village:dusk
aspect: "16:9"
output_size: "1920x1080"
chroma: false
status: idea
---

# UI Mockup: ui_settings (설정 / Settings & Pause modal) — dusk

```prompt
# UI Mockup: Settings & Pause modal — dusk village backdrop

An illustrated UI mockup screenshot of a premium mobile JRPG / gacha-banner game's settings / pause screen — anime-painted UI panel overlay on top of the frontier village hub at dusk. The settings modal is shared between Title screen and in-game Pause. 16:9 wide.

**STYLE LOCK**: Same illustrated world as project's character roster splash art and hub baseline. Refs: character portrait + hub `town_frontier_village:dusk`. Anime-leaning painterly base, sparse cel-edge, vivid harmonious palette. NOT photoreal, NOT wireframe.

**FIGURE GUARD**: Character ref is stylization only. No character figure in this modal.

## Composition

Layered: hub backdrop dimmed ~35% + frosted glass modal ~70% × ~75% centered (slightly smaller than other modals — settings is a quieter screen).

### Top — Modal Header (~8%)

A simple header bar: title placeholder ("⬢⬢⬢⬢" abstract glyph for "설정" / "Settings") in center, close X on right. A subtle gold underline.

### Left — Category Sidebar (~25% of modal width)

Vertical category tabs (top to bottom):
- **Graphics** (display icon glyph) — currently selected with gold rim
- **Audio** (sound wave icon)
- **Control** (gamepad icon)
- **Language** (flag icon)
- **Account** (avatar icon)
- **Credits** (book icon)

Each tab is a horizontal strip with icon + placeholder name (abstract glyphs).

### Main Section — Option List (~55% of modal width)

A vertical list of settings options (Graphics category currently shown):
- **Quality preset** — segmented control (Low / Medium / High / Ultra) — Medium selected with gold fill
- **Resolution** — dropdown (placeholder text + chevron)
- **Frame rate cap** — slider (warm gold thumb at 60 FPS placeholder)
- **VSync** — toggle switch (on, gold)
- **Brightness** — slider (warm gradient bar with thumb)
- **HDR** — toggle (off, grey)
- **UI Scale** — slider (warm thumb)
- **Subtitle size** — segmented control (S/M/L)

Each option has:
- Left: placeholder option name (abstract glyphs)
- Right: control widget (slider / toggle / dropdown / segmented)
- Optional small info icon for tooltip hint

The list has a subtle scroll indicator on the right.

### Right — Action Bar (~20% of modal width)

Vertical column with:
- **Reset to Default** button (cool grey, small)
- **Apply** button (warm gold, prominent CTA)
- **Cancel** button (deep navy)

Below the buttons: a small caption indicator showing "unsaved changes" placeholder (warm orange dot if there are pending changes).

## Mood

**Calm utility.** Settings feels like a quiet workbench — clean, organized, low ceremony. The dusk village paused outside. Warm parchment + frosted glass + restrained gold accents. The player should feel "I can adjust everything I need without friction."

## Lighting / Palette

- Background dim ~35%, cool slate
- Modal: warm parchment frame with frosted-glass interior, slightly smaller scale
- Selected category: warm gold rim
- Slider thumbs: warm gold
- Toggle on: warm gold; off: cool grey
- Apply button: warm gold (primary CTA)
- Same painterly anime palette as other modals

## What this is NOT

- NOT wireframe, NOT photoreal screenshot, NOT legible text
- NOT cluttered (option list spacious)
- NOT a different stylization from the hub baseline
- NOT a real working UI with legible labels — all text is abstract glyph blocks
- NOT skeuomorphic 3D widgets — clean stylized painterly UI controls
```
