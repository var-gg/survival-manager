---
slug: ui_passive_board--dusk
kind: environment_site
subject_id: ui_passive_board
variant: dusk
emotion: default
refs:
  - hero_dawn_priest:portrait_full
  - town_frontier_village:dusk
aspect: "16:9"
output_size: "1920x1080"
chroma: false
status: idea
---

# UI Mockup: ui_passive_board (패시브 보드 / Passive Board) — dusk

```prompt
# UI Mockup: Passive Board modal — dusk village backdrop

An illustrated UI mockup screenshot of a premium mobile JRPG / gacha-banner game's passive skill tree screen — anime-painted UI panel overlay on top of the frontier village hub at dusk. Reads as a class-locked passive board (vanguard / duelist / ranger / mystic), 4 boards available via top tabs. 16:9 wide.

**STYLE LOCK (CRITICAL)**: Same illustrated world as the project's character roster splash art and the hub baseline. Two reference images attached: (1) a character portrait, and (2) the hub baseline `town_frontier_village:dusk`. Anime-leaning painterly base, sparse cel-edge, vivid harmonious palette, hand-drawn brushwork. NOT photoreal, NOT concept-art realism, NOT a wireframe.

**FIGURE GUARD**: Character ref is for stylization reference only. No character portrait foreground.

## Composition

Layered: Town hub backdrop dimmed ~35% + frosted glass modal panel ~80% × ~85% centered.

### Top — 4 Class Tabs (~10% of modal height)

Horizontal tab bar with **4 tabs**:
- Tab 1: Vanguard (steel-grey + warm wood, shield icon)
- Tab 2: Duelist (deep navy + silver, twin blade icon) — currently active, gold underline
- Tab 3: Ranger (forest green + leather brown, bow icon)
- Tab 4: Mystic (deep violet + crystal, staff icon)

Each tab is a labeled rectangle with stylized class icon glyph + placeholder name (abstract glyphs).

### Main Section — Node Grid (~70% of modal height)

A **hexagonal passive node tree** filling the central area. The structure:
- Central root node (largest, glowing warm) — class signature node
- 6-8 inner ring nodes connected by thin painterly line paths
- 12-15 outer ring nodes
- Some nodes are connected by branching paths

Node states (visually distinct):
- **Active nodes** (~5-6): warm gold inner glow, full color, connected by warm gold path lines from root
- **Available nodes** (~3-4): cool blue rim, can be activated next, paths from active nodes glow faintly
- **Locked nodes** (rest): dim grey silhouette, paths are thin grey lines

Each node has a small symbolic icon inside (a sword, a shield, a flame, a heart, a crit-star, etc.) representing its passive effect. Painterly hand-drawn icon style.

The grid feels like a **constellation map** — nodes scattered with intentional asymmetry, not a rigid grid. Cool dark background inside the modal interior with subtle warm path glow connecting active nodes.

### Right Side Panel — Selected Node Detail (~25% of modal width, overlaid on right)

A vertical detail panel:
- Top: **selected node icon** (large, painterly) + tier indicator
- Middle: placeholder name strip + 3-4 line effect description (abstract glyphs)
- Bottom: **Activate CTA** (warm gold button, abstract glyph text) + node point cost indicator (small star or rune icon + numeric placeholder)

If the selected node is already active: show "Active" indicator instead of Activate CTA.

### Bottom Bar — Node Points Summary (~5% of modal height)

A thin status bar:
- Left: total node points spent / total available (abstract glyph numerics)
- Center: respec hint (small placeholder)
- Right: close X mirror button

## Mood

**Constellation craft.** The board feels like a personal star chart — the player has lit up their constellation through gameplay. Cool dark interior with warm gold paths and node glows. The dusk village outside is paused. Player should feel "I've shaped this character's growth path."

## Lighting / Palette

- Background dim ~35%, cool slate
- Modal: dark interior with warm node glows (NOT bright modal — this is a constellation in dusk)
- Active nodes: warm gold inner glow + connecting paths
- Available nodes: cool blue rim glow
- Locked nodes: dim grey
- Selected node panel: warm parchment frame
- Painterly anime icon style for node symbols

## What this is NOT

- NOT a wireframe diagram, NOT photoreal screenshot, NOT legible text
- NOT a rigid grid (constellation feel, asymmetric)
- NOT cluttered (path lines clean, nodes spaced)
- NOT a different stylization from the hub baseline
- NOT magical animated effects (still illustration only)
```
